
class File {

	constructor() {
		// temporary
		this._file = window.bluePrint.selectSingleNode(`//file`);
		this._tempo = 120;
	}

	dispatch(event) {
		let APP = eniac,
			name,
			value,
			str;
		switch (event.type) {
			case "reset-sheet-names":
				break;
		}
	}

	toBlob() {
		
	}

	get isDirty() {
		
	}

	undo() {
		
	}

	redo() {
		
	}

}
