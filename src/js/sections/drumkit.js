
// defjam.drumkit

{
	init() {

	},
	dispatch(event) {
		let APP = defjam,
			Self = APP.drumkit,
			name,
			value,
			el;
		// console.log(event);
		switch (event.type) {
			case "play-pad":
				
				break;
		}
	}
}
